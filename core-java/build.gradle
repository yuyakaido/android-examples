apply plugin: 'java-library'
apply plugin: 'kotlin'

dependencies {
  // Kotlin
  api libs.kotlin.core.stdlib

  // Networking
  api libs.networking.retrofit.core
  api libs.networking.retrofit.moshi
}
